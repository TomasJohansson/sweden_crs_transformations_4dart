# Files and directories created by pub
.dart_tool/
.packages

# Omit commiting pubspec.lock for library packages:
# https://dart.dev/guides/libraries/private-files#pubspeclock
pubspec.lock

# Conventional directory for build outputs
build/

# Directory created by dartdoc
doc/api/


# the "test" directory can temporary be renamed to "test_temp" to avoid publishing the tests when running 'dart pub publish'
test_temp/
# https://dart.dev/tools/pub/publishing#what-files-are-published
#   If you’re using Git, any files ignored by your .gitignore file.